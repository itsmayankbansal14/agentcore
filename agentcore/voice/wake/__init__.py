"""AgentCore — voice.wake
Wake-word seam. Per product direction the wake word is DEFERRED until the
core speak→hear pipeline is reliable — so the only implementation here is the
disabled detector, and the seam is the place a real one will land later.
"""
