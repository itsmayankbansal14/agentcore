"""AgentCore — tools/workflows/__init__.py
Capability-validation workflow tools. Each is a REAL implementation, registered
in the Tool Registry, so any workflow runs through the full pipeline:
Planner → Executor → PermissionManager → Registry → Tool → Observer → Memory → History.
"""
