[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q --strict-markers
markers =
    integration: exercises multiple components of the real runtime end-to-end
    unit: isolated component test

filterwarnings =
    ignore::DeprecationWarning
