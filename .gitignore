# Secrets — never commit
.env

# Runtime data + databases (per review: ignore *.db, *.db-shm, *.db-wal)
data/
*.db
*.db-shm
*.db-wal
*.backup-*.db

# Logs
logs/

# Build outputs (PyInstaller) — never commit these
dist/
build/
*.spec

# Python
__pycache__/
*.pyc
.venv/

# Editor / OS
.vscode/
.DS_Store
Thumbs.db
